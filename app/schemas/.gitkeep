# Room Database Schema 目录

此目录存储 Room 数据库的 schema 文件。

## 说明
- Room 会在编译时自动生成每个版本的数据库结构 JSON 文件
- 文件路径格式: `com.fam4k007.videoplayer.database.VideoDatabase/{version}.json`
- 这些文件应该提交到版本控制，用于：
  - 追踪数据库结构变更历史
  - 对比不同版本的差异
  - 验证 Migration 的正确性
  - 团队协作时了解数据库演变过程

## 当前版本
- Version 1: video_cache 表（视频缓存）
- Version 2: 添加 playback_history 表（播放历史）
- Version 3: 为 video_cache 添加索引优化

## 注意
首次编译项目后，Room 会自动生成 schema 文件到此目录。
