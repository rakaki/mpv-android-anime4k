# Activity 切换动画预览

本文档提供项目中所有 Activity 切换动画的可视化说明，无需编译即可理解动画效果。

---

## 📱 当前使用的动画组合

### 1️⃣ 右滑进入（最常用）
**使用场景**：打开新页面（如：主页 → 设置页）

```
进入动画：slide_in_right.xml  ┃  退出动画：slide_out_left.xml
```

**视觉效果：**
```
旧页面                      新页面
━━━━━━━━                  ━━━━━━━━
│      │                  │      │
│  旧  │ ←────────────── │  新  │ (从右侧滑入)
│ 页面 │ (向左移动+缩小)  │ 页面 │
│      │                  │      │
━━━━━━━━                  ━━━━━━━━

动画时长：300ms
特效：新页面从右侧滑入 + 淡入
     旧页面向左移动 + 轻微缩小 + 淡出
```

**代码使用：**
```kotlin
overridePendingTransition(R.anim.slide_in_right, R.anim.slide_out_left)
```

---

### 2️⃣ 左滑返回
**使用场景**：返回上一页（如：设置页 → 主页）

```
进入动画：slide_in_left.xml  ┃  退出动画：slide_out_right.xml
```

**视觉效果：**
```
新页面                      旧页面
━━━━━━━━                  ━━━━━━━━
│      │                  │      │
│  旧  │ ───────────────► │  新  │ (向右滑出)
│ 页面 │ (从左侧滑入)     │ 页面 │
│      │                  │      │
━━━━━━━━                  ━━━━━━━━

动画时长：300ms
特效：旧页面从左侧滑入
     新页面向右滑出 + 淡出
```

**代码使用：**
```kotlin
overridePendingTransition(R.anim.slide_in_left, R.anim.slide_out_right)
```

---

### 3️⃣ 底部弹出
**使用场景**：对话框式页面、设置面板

```
进入动画：slide_in_bottom.xml  ┃  退出动画：slide_out_down.xml
```

**视觉效果：**
```
                新页面
                ━━━━━━━━
                │      │
                │  新  │ 
                │ 页面 │ ↑ (从底部向上滑入)
                │      │
━━━━━━━━━━━━  ━━━━━━━━
│  旧页面   │  
━━━━━━━━━━━━

动画时长：300ms
特效：新页面从底部向上滑入
     退出时向下滑出
```

**代码使用：**
```kotlin
overridePendingTransition(R.anim.slide_in_bottom, R.anim.slide_out_down)
```

---

### 4️⃣ 顶部滑入
**使用场景**：通知、下拉展开

```
进入动画：slide_in_up.xml  ┃  退出动画：no_anim.xml
```

**视觉效果：**
```
━━━━━━━━━━━━  
│  新页面   │  ↓ (从顶部向下滑入)
━━━━━━━━━━━━  
                ━━━━━━━━
                │      │
                │  旧  │ 
                │ 页面 │
                │      │
                ━━━━━━━━

动画时长：300ms
```

---

### 5️⃣ 淡入淡出
**使用场景**：轻量级切换、同级页面切换

```
进入动画：fade_in.xml  ┃  退出动画：fade_out.xml
```

**视觉效果：**
```
━━━━━━━━                  ━━━━━━━━
│      │                  │      │
│  旧  │  渐变效果 ░░░░░  │  新  │
│ 页面 │  (透明度变化)    │ 页面 │
│      │                  │      │
━━━━━━━━                  ━━━━━━━━

动画时长：300ms
特效：纯透明度变化，无位移
     旧页面：透明度 1.0 → 0.0
     新页面：透明度 0.0 → 1.0
```

**代码使用：**
```kotlin
overridePendingTransition(R.anim.fade_in, R.anim.fade_out)
```

---

### 6️⃣ 缩放动画
**使用场景**：弹窗、对话框

```
进入动画：scale_in.xml  ┃  退出动画：scale_out.xml
```

**视觉效果：**
```
        ·                   ━━━━━━━━
        ·                  ┃      ┃
        ·  →  放大  →     ┃  新  ┃
        ·                  ┃ 页面 ┃
        ·                  ┃      ┃
      (点)                  ━━━━━━━━

动画时长：200-300ms
特效：从中心点放大 + 淡入
     退出时缩小到中心点 + 淡出
```

---

### 7️⃣ 弹窗动画
**使用场景**：Popup菜单、快速选择

```
进入动画：popup_enter.xml  ┃  退出动画：popup_exit.xml
```

**视觉效果：**
```
从顶部弹出 + 淡入
━━━━━━━━
│      │ ↓ 
│ 菜单 │
━━━━━━━━

动画时长：200ms
特效：从顶部快速滑入 + 淡入
```

---

### 8️⃣ 无动画
**使用场景**：需要瞬间切换的情况

```
动画：no_anim.xml
```

**视觉效果：**
```
瞬间切换，无过渡效果
```

---

## 🎨 推荐的动画组合方案

### 方案 A：现代滑动（当前使用）
- **进入新页面**：`slide_in_right` + `slide_out_left`
- **返回上页**：`slide_in_left` + `slide_out_right`
- **特点**：流畅、有方向感、iOS 风格

### 方案 B：优雅淡入
- **进入新页面**：`fade_in` + `slide_out_left`
- **返回上页**：`slide_in_left` + `fade_out`
- **特点**：轻盈、优雅、Material Design 风格

### 方案 C：弹性效果
- **进入新页面**：`scale_in` + `fade_out`
- **返回上页**：`fade_in` + `scale_out`
- **特点**：动感、现代

### 方案 D：对话框式
- **进入新页面**：`slide_in_bottom` + `fade_out`
- **返回上页**：`fade_in` + `slide_out_down`
- **特点**：适合设置、详情页

---

## 📊 动画参数对照表

| 动画文件 | 时长 | 位移 | 透明度 | 缩放 | 插值器 |
|---------|------|------|--------|------|--------|
| slide_in_right | 300ms | 100%→0% | 0.8→1.0 | - | fast_out_slow_in |
| slide_out_left | 300ms | 0%→-25% | 1.0→0.6 | 1.0→0.95 | fast_out_slow_in |
| slide_in_left | 300ms | -100%→0% | 0.8→1.0 | - | fast_out_slow_in |
| slide_out_right | 300ms | 0%→100% | 1.0→0.0 | - | fast_out_slow_in |
| slide_in_bottom | 300ms | Y:100%→0% | - | - | fast_out_slow_in |
| slide_out_down | 300ms | Y:0%→100% | - | - | fast_out_slow_in |
| slide_in_up | 300ms | Y:-100%→0% | - | - | fast_out_slow_in |
| fade_in | 300ms | - | 0.0→1.0 | - | - |
| fade_out | 300ms | - | 1.0→0.0 | - | - |
| scale_in | 200ms | - | 0.0→1.0 | 0.7→1.0 | accelerate |
| scale_out | 200ms | - | 1.0→0.0 | 1.0→0.7 | accelerate |

---

## 🔧 如何修改动画

### 调整动画速度
在动画XML文件中修改 `android:duration` 值：
- **快速**：150-200ms
- **正常**：250-300ms（推荐）
- **慢速**：400-500ms

### 调整动画曲线（插值器）
常用插值器：
- `@android:interpolator/accelerate` - 加速
- `@android:interpolator/decelerate` - 减速
- `@android:interpolator/accelerate_decelerate` - 先加速后减速
- `@android:interpolator/fast_out_slow_in` - 快出慢入（推荐）
- `@android:interpolator/linear` - 线性

---

## 💡 使用建议

1. **保持一致性**：整个应用使用相同的动画风格
2. **时长适中**：300ms 是最佳平衡点
3. **方向明确**：使用滑动动画增强导航感
4. **性能优先**：避免复杂动画组合
5. **测试体验**：在真机上测试动画流畅度

---

## 📝 项目当前使用情况

```kotlin
// VideoBrowserActivity.kt
overridePendingTransition(R.anim.slide_in_right, R.anim.slide_out_left)  // 打开
overridePendingTransition(R.anim.slide_in_left, R.anim.slide_out_right)  // 返回

// VideoListActivity.kt  
overridePendingTransition(R.anim.slide_in_right, R.anim.slide_out_left)  // 打开
overridePendingTransition(R.anim.slide_in_left, R.anim.slide_out_right)  // 返回
```

当前采用**方案 A：现代滑动**风格，统一的左右滑动体验。
